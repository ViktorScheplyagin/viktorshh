export const API_ROOT = "http://localhost:3000/api";

export enum API_ROUTES {
  BIO = `${API_ROOT}/bio`,
  EDUCATION = `${API_ROOT}/education`,
  POSITIONS = `${API_ROOT}/positions`,
  SKILLS = `${API_ROOT}/skills`,
}
